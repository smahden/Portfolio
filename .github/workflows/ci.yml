name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  taskflow:
    name: TaskFlow tests (Node)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/taskflow
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm test

  shoplite:
    name: ShopLite tests (Python)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/shoplite
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pytest -q
